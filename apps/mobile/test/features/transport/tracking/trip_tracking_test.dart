import 'package:flutter_test/flutter_test.dart'; import '../transport_test_data.dart'; import '../../../../lib/features/transport/transport_api.dart'; import '../../../../lib/features/transport/tracking/location_update_queue.dart'; import '../../../../lib/features/transport/tracking/trip_tracking_repository.dart'; void main() { test('trip tracking repository maps offline location to queue', () async { final repo = TripTrackingRepository(api: TransportApiClient(schoolAccountId: 'school-1', authToken: 'token'), locationQueue: LocationUpdateQueue()); final result = await repo.submitLocation(testLocation(), online: false); expect(result, 'queued'); }); }
