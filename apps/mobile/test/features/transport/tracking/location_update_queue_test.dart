import 'package:flutter_test/flutter_test.dart'; import '../transport_test_data.dart'; import '../../../../lib/features/transport/tracking/location_update_queue.dart'; void main() { test('location queue deduplicates client location id', () async { final queue = LocationUpdateQueue(); final update = testLocation(); await queue.enqueue(update); await queue.enqueue(update); expect(await queue.pending(), hasLength(1)); }); }
