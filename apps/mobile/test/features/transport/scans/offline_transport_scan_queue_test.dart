import 'package:flutter_test/flutter_test.dart'; import '../transport_test_data.dart'; import '../../../../lib/features/transport/scans/offline_transport_scan_queue.dart'; void main() { test('offline scan queue deduplicates client scan id', () async { final queue = OfflineTransportScanQueue(); final scan = testTransportScan(); await queue.enqueue(scan); await queue.enqueue(scan); expect(await queue.pending(), hasLength(1)); }); }
