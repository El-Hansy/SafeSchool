import 'package:flutter/material.dart';
class CertificateManagementScreen extends StatelessWidget { const CertificateManagementScreen({super.key}); @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Certificates'))); }
