import 'package:flutter/material.dart';
class DocumentStorageScreen extends StatelessWidget { const DocumentStorageScreen({super.key}); @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Documents'))); }
