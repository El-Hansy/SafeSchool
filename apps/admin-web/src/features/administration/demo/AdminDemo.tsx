import type { CSSProperties } from "react";
import { adminDemoData } from "../api/adminApi";

const styles: Record<string, CSSProperties> = { page: { minHeight: "100vh", background: "#f3f5f7", color: "#111827", padding: 32 }, header: { display: "flex", justifyContent: "space-between", gap: 24, alignItems: "start", marginBottom: 24 }, eyebrow: { fontSize: 13, fontWeight: 800, color: "#5b6472", textTransform: "uppercase" }, title: { fontSize: 40, margin: "6px 0" }, subtitle: { color: "#4b5563", maxWidth: 900, lineHeight: 1.5, fontSize: 18 }, badge: { border: "1px solid #bfdbfe", background: "#dbeafe", color: "#1d4ed8", padding: "10px 14px", borderRadius: 6, fontWeight: 800 }, nav: { display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 22 }, link: { border: "1px solid #cbd5e1", background: "#fff", borderRadius: 6, padding: "9px 12px", color: "#1f2937", textDecoration: "none", fontWeight: 800 }, grid: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 14, marginBottom: 22 }, panel: { background: "#fff", border: "1px solid #d5dde6", borderRadius: 8, padding: 20, boxShadow: "0 1px 2px rgba(15,23,42,.06)" }, row: { padding: "10px 0", borderBottom: "1px solid #e5e7eb", color: "#4b5563" } };

export function AdminDemo({ view = "overview", audience = "school" }: { view?: string; audience?: string }) {
  const metrics = adminDemoData.metrics;
  return <main style={styles.page}><header style={styles.header}><div><div style={styles.eyebrow}>SafeSchool 011</div><h1 style={styles.title}>Admin, Audit & Observability</h1><p style={styles.subtitle}>Tenant feature configuration, dashboards, audit trail, controlled exports, metrics, alerts, incidents, operational exceptions, and review evidence.</p></div><div style={styles.badge}>Demo data</div></header><nav style={styles.nav}><a style={styles.link} href="/admin">Dashboard</a><a style={styles.link} href="/admin/configuration">Configuration</a><a style={styles.link} href="/admin/audit">Audit</a><a style={styles.link} href="/admin/monitoring">Monitoring</a><a style={styles.link} href="/admin/alerts">Alerts</a><a style={styles.link} href="/admin/incidents">Incidents</a></nav><section style={styles.grid}>{metrics.map((metric) => <article key={metric.label} style={styles.panel}><strong style={{ fontSize: 32 }}>{metric.value}</strong><div>{metric.label}</div></article>)}</section><section style={styles.panel}><h2>{view} - {audience}</h2>{adminDemoData.rows.map((row) => <div key={row} style={styles.row}>{row}</div>)}<p style={{ color: "#64748b" }}>Tenant-scoped, permission-checked, audited, and side-effect bounded.</p></section></main>;
}
