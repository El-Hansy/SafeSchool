import type { TransportRouteSummary } from "./routesApi"; export function RouteForm({ route }: { route?: TransportRouteSummary }) { return <form aria-label="Transport route"><label>Route code<input name="routeCode" defaultValue={route?.routeCode ?? ""} /></label><button type="submit">Save</button></form>; } export function StopForm() { return <form aria-label="Transport stop"><label>Stop code<input name="stopCode" /></label><button type="submit">Save</button></form>; } export function RouteStatusBadge({ status }: { status: string }) { return <span data-status={status}>{status}</span>; }
