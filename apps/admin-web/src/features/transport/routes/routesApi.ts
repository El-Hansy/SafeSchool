export const transportRouteRoutes = { list: "/routes", create: "/routes", stops: "/stops", sequence: (routeId: string) => `/routes/${routeId}/stop-sequences`, version: (routeId: string, version: string) => `/routes/${routeId}/versions/${version}`, trace: (routeId: string) => `/routes/${routeId}/trace` }; export type TransportRouteSummary = { routeCode: string; routeStatus: "Draft" | "Active" | "Suspended" | "Retired" };
