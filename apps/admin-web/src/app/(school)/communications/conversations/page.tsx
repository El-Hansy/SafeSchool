import { OperationalRoutePage } from "@/features/home";

export default function Page() {
  return (
    <OperationalRoutePage
      area="Communications"
      title="Conversations"
      detail="Review direct conversations, moderation queues, participant scope, unread status, and escalation needs."
    />
  );
}
