import { OperationalRoutePage } from "@/features/home";

export default function Page() {
  return (
    <OperationalRoutePage
      area="Communications"
      title="New Conversation"
      detail="Start a permission-scoped direct conversation using approved participants, reason capture, and moderation controls."
    />
  );
}
