import { OperationalRoutePage } from "@/features/home";

export default function Page() {
  return (
    <OperationalRoutePage
      area="Administration / Feature Configuration"
      title="Tenant Feature Controls"
      detail="Manage role-visible feature switches, phase availability, dependency warnings, and audit-backed configuration state."
    />
  );
}
