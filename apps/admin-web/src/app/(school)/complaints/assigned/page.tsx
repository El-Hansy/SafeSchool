import { OperationalRoutePage } from "@/features/home";

export default function Page() {
  return (
    <OperationalRoutePage
      area="Complaints"
      title="Assigned Complaints"
      detail="Review staff-owned complaint work queues with priorities, due times, escalation indicators, and closure progress."
    />
  );
}
