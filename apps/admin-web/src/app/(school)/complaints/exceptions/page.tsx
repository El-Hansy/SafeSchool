import { OperationalRoutePage } from "@/features/home";

export default function Page() {
  return (
    <OperationalRoutePage
      area="Complaints"
      title="Complaint Exceptions"
      detail="Review duplicate, invalid, blocked, overdue, or audit-risk complaints with required staff follow-up."
    />
  );
}
