# Contract Test: Mobile Workspaces

- `GET /api/v1/mobile/workspaces` returns only visible workspaces.
- `GET /api/v1/mobile/workspaces/{workspaceCode}` includes source-domain
  action ownership and text direction.
